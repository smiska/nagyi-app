import React from 'react'
import App from './app'
//import { shallow } from 'enzyme';

window.alert = jest.fn(() => ({}));

describe('app tests', ()=> {

    it('renders', () => {
        expect(App).toBe()
    })
})
